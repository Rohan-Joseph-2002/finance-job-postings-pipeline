[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-job-postings-pipeline"
version = "0.1.0"
description = "Standalone pipeline for constructing, matching, and analyzing finance-related job posting samples from Lightcast-style data."
readme = "README.md"
requires-python = ">=3.9.6"
dependencies = [
    "pandas>=2.2"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
